# v0.6.0

### Added

- 동기화 제외 기능 — 같은 폴더 안에서 로컬 전용 문서를 sync 대상에서 뺄 수 있음
  - `<base>/.confluence-syncignore` (`.gitignore` 방식 패턴 파일)
  - `--exclude <glob>` 옵션(반복 가능)
  - 매칭은 `ignore`(gitignore 스펙) 사용 — 부정(`!`)·`**` 등 지원. 제외 문서만 있던 폴더는 생성 안 됨

### Changed

- 런타임 의존성에 `ignore` 추가
